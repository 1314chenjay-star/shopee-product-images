name: TinySnow Default Branch Supervisor

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/tinysnow-default-branch-supervisor.yml'
  schedule:
    - cron: '7,22,37,52 * * * *'
  workflow_dispatch:

permissions:
  contents: write
  actions: read

concurrency:
  group: tinysnow-default-branch-supervisor
  cancel-in-progress: false

jobs:
  c5-3-delta-recovery:
    permissions:
      contents: write
      actions: read
    uses: 1314chenjay-star/shopee-product-images/.github/workflows/v4c-c5-3-delta-recovery-bridge.yml@v4c-universal-product-engine
    secrets: inherit
